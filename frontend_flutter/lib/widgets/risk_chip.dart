import 'package:flutter/material.dart';

class RiskChip extends StatelessWidget {
  const RiskChip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Chip(
      label: Text('Risk'),
    );
  }
}
